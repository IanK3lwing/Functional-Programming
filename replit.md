# Laboratório de Programação Funcional

Ambiente de estudos para Java e Scala, sem frontend.

## Executar

```bash
cd functional-programming-lab
sbt compile
```

Os fontes ficam em `src/main/java` e `src/main/scala`. Os testes ficam em
`src/test/java` e `src/test/scala`.
