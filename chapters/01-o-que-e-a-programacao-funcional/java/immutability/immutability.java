public class immutability {
  public static void main(String[] args) { 
   // Pf é o coneito de imutabilidade, que diz que algo não pode ser mudado.
    // o que significa que se temos uma variável ela terá seu valor até o fim do programa
    // Mas isso não significa que se temos um programa escrito com PF com uma idade e se passar
    // uma ano a idade será a mesma? Isso seria um absurdo! E na PF existe algumas formas de se lidar
    // com isso

    for (int i = 0; i < 100; i++) {
      System.out.println(i);
    }

    // No loop estamos mudando o estado da variável "i"
    // uma abordagem super comum na programação, mas como poderiamos
    // fazer esse código de forma funcional? veja no arquivo @immutabilyTwo.java 

    // CONTINUE IN immutabilityTwo.java....
    
  }

}

